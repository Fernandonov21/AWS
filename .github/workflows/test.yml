name: Deploy to EC2 (QA)

on:
  push:
    branches:
      - test    # Este flujo se ejecutará cuando hagas push a la rama 'test'

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        ec2_instance: 
          - ec2-54-88-190-219.compute-1.amazonaws.com

    steps:
    # Paso 1: Clonar el código
    - name: Checkout code
      uses: actions/checkout@v2

    # Paso 2: Configurar Docker
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v2

    # Paso 3: Iniciar sesión en Docker Hub
    - name: Log in to Docker Hub
      uses: docker/login-action@v2
      with:
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    # Paso 4: Construir la imagen Docker
    - name: Build Docker image
      run: |
        docker build -t ${{ secrets.DOCKER_USERNAME }}/php-app:latest .

    # Paso 5: Subir la imagen Docker a Docker Hub
    - name: Push Docker image
      run: |
        docker push ${{ secrets.DOCKER_USERNAME }}/php-app:latest

    # Paso 6: Desplegar en la instancia EC2 de QA
    - name: Deploy to QA EC2 instance
      run: |
        echo "${{ secrets.EC2_SSH_KEY }}" > key.pem
        chmod 400 key.pem
        ssh -o StrictHostKeyChecking=no -i key.pem ec2-user@${{ matrix.ec2_instance }} << EOF
          # Detener el contenedor anterior
          docker stop php-app || true
          docker rm php-app || true

          # Ejecutar el nuevo contenedor
          docker run -d --name php-app -p 80:80 ${{ secrets.DOCKER_USERNAME }}/php-app:latest
        EOF

    # Paso 7: Eliminar la clave SSH
    - name: Clean up SSH key
      run: rm key.pem
